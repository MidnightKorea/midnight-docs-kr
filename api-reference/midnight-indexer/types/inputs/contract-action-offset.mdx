---
id: contract-action-offset
title: ContractActionOffset
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)"
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)"
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

블록 offset 또는 트랜잭션 offset입니다.

```graphql
input ContractActionOffset {
  blockOffset: BlockOffset
  transactionOffset: TransactionOffset
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>ContractActionOffset.<b>blockOffset</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>BlockOffset</b></code>](/midnight-indexer/types/inputs/block-offset.mdx) <Badge class="badge badge--secondary " text="input"/>

블록 해시 또는 블록 높이입니다.

#### [<code style={{ fontWeight: 'normal' }}>ContractActionOffset.<b>transactionOffset</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>TransactionOffset</b></code>](/midnight-indexer/types/inputs/transaction-offset.mdx) <Badge class="badge badge--secondary " text="input"/>

트랜잭션 해시 또는 트랜잭션 식별자입니다.

### Member Of

[`contractAction`](/midnight-indexer/operations/queries/contract-action.mdx) <Badge class="badge badge--secondary badge--relation" text="query"/>
