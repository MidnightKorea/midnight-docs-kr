---
id: contract-balance
title: ContractBalance
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)"
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)"
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

컨트랙트가 보유한 토큰 잔고를 나타냅니다.
GraphQL API를 통해 노출되며, 클라이언트가 모든 컨트랙트 액션(Deploy, Call, Update)의 비차폐 토큰 잔고를 조회할 수 있습니다.

```graphql
type ContractBalance {
  tokenType: HexEncoded!
  amount: String!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>ContractBalance.<b>tokenType</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>HexEncoded!</b></code>](/midnight-indexer/types/scalars/hex-encoded.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

16진수로 인코딩된 토큰 타입 식별자입니다.

#### [<code style={{ fontWeight: 'normal' }}>ContractBalance.<b>amount</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>String!</b></code>](/midnight-indexer/types/scalars/string.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

큰 정수 값(최대 16바이트)을 지원하기 위해 문자열로 표현된 잔고 금액입니다.

### Member Of

[`ContractAction`](/midnight-indexer/types/interfaces/contract-action.mdx) <Badge class="badge badge--secondary badge--relation" text="interface"/><Bullet />[`ContractCall`](/midnight-indexer/types/objects/contract-call.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`ContractDeploy`](/midnight-indexer/types/objects/contract-deploy.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`ContractUpdate`](/midnight-indexer/types/objects/contract-update.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
