---
id: contract-deploy
title: ContractDeploy
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)"
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)"
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

컨트랙트 배포입니다.

```graphql
type ContractDeploy implements ContractAction {
  address: HexEncoded!
  state: HexEncoded!
  zswapState: HexEncoded!
  transaction: Transaction!
  unshieldedBalances: [ContractBalance!]!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>ContractDeploy.<b>address</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>HexEncoded!</b></code>](/midnight-indexer/types/scalars/hex-encoded.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

16진수로 인코딩된 직렬화 주소입니다.

#### [<code style={{ fontWeight: 'normal' }}>ContractDeploy.<b>state</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>HexEncoded!</b></code>](/midnight-indexer/types/scalars/hex-encoded.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

16진수로 인코딩된 직렬화 상태입니다.

#### [<code style={{ fontWeight: 'normal' }}>ContractDeploy.<b>zswapState</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>HexEncoded!</b></code>](/midnight-indexer/types/scalars/hex-encoded.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

16진수로 인코딩된 컨트랙트별 zswap 직렬화 상태입니다.

#### [<code style={{ fontWeight: 'normal' }}>ContractDeploy.<b>transaction</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Transaction!</b></code>](/midnight-indexer/types/interfaces/transaction.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="interface"/>

이 컨트랙트 배포의 트랜잭션입니다.

#### [<code style={{ fontWeight: 'normal' }}>ContractDeploy.<b>unshieldedBalances</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>[ContractBalance!]!</b></code>](/midnight-indexer/types/objects/contract-balance.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="object"/>

이 컨트랙트가 보유한 비차폐 토큰 잔고입니다.

### Interfaces

#### [<code style={{ fontWeight: 'normal' }}><b>ContractAction</b></code>](/midnight-indexer/types/interfaces/contract-action.mdx) <Badge class="badge badge--secondary " text="interface"/>

컨트랙트 액션입니다.

### Member Of

[`ContractCall`](/midnight-indexer/types/objects/contract-call.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
