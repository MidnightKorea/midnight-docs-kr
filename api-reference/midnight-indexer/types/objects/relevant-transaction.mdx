---
id: relevant-transaction
title: RelevantTransaction
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)"
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)"
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

구독 중인 지갑에 관련된 트랜잭션이며, 선택적으로 축소된 머클 트리를 포함합니다.

```graphql
type RelevantTransaction {
  transaction: RegularTransaction!
  collapsedMerkleTree: CollapsedMerkleTree
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>RelevantTransaction.<b>transaction</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>RegularTransaction!</b></code>](/midnight-indexer/types/objects/regular-transaction.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="object"/>

구독 중인 지갑에 관련된 트랜잭션입니다.

#### [<code style={{ fontWeight: 'normal' }}>RelevantTransaction.<b>collapsedMerkleTree</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>CollapsedMerkleTree</b></code>](/midnight-indexer/types/objects/collapsed-merkle-tree.mdx) <Badge class="badge badge--secondary " text="object"/>

선택적 축소 머클 트리입니다.

### Implemented By

[`ShieldedTransactionsEvent`](/midnight-indexer/types/unions/shielded-transactions-event.mdx) <Badge class="badge badge--secondary badge--relation" text="union"/>
