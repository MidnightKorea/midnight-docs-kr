---
id: shielded-transactions-progress
title: ShieldedTransactionsProgress
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)"
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)"
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

차폐 트랜잭션 인덱싱 진행 상태 정보입니다.

```graphql
type ShieldedTransactionsProgress {
  highestEndIndex: Int!
  highestCheckedEndIndex: Int!
  highestRelevantEndIndex: Int!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>ShieldedTransactionsProgress.<b>highestEndIndex</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Int!</b></code>](/midnight-indexer/types/scalars/int.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

전체 트랜잭션의 최대 zswap 상태 종료 인덱스입니다(`Transaction`의 `endIndex` 참조). 블록체인의 알려진 상태를 나타냅니다. 0인 경우(가능성 극히 낮음) 아직 차폐 트랜잭션이 인덱싱되지 않았음을 의미합니다.

#### [<code style={{ fontWeight: 'normal' }}>ShieldedTransactionsProgress.<b>highestCheckedEndIndex</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Int!</b></code>](/midnight-indexer/types/scalars/int.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

관련성이 확인된 전체 트랜잭션의 최대 zswap 상태 종료 인덱스입니다(`Transaction`의 `endIndex` 참조). 처음에는 `highest_end_index`보다 작으며, 특정 지갑의 인덱싱이 완료되면 같아집니다. 0인 경우(가능성 매우 낮음) 아직 구독한 지갑이 없고 인덱싱이 시작되지 않았음을 의미합니다.

#### [<code style={{ fontWeight: 'normal' }}>ShieldedTransactionsProgress.<b>highestRelevantEndIndex</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Int!</b></code>](/midnight-indexer/types/scalars/int.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

구독 중인 지갑에 관련된 전체 트랜잭션의 최대 zswap 상태 종료 인덱스입니다(`Transaction`의 `endIndex` 참조). 마지막으로 확인된 트랜잭션이 해당 지갑에 관련되지 않는 한, 보통 `highest_checked_end_index`보다 작습니다. 0인 경우 해당 지갑에 관련된 트랜잭션이 아직 인덱싱되지 않았음을 의미합니다.

### Implemented By

[`ShieldedTransactionsEvent`](/midnight-indexer/types/unions/shielded-transactions-event.mdx) <Badge class="badge badge--secondary badge--relation" text="union"/>
