---
id: system-parameters
title: SystemParameters
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)"
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)"
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

특정 블록 높이에서의 시스템 파라미터입니다.

```graphql
type SystemParameters {
  dParameter: DParameter!
  termsAndConditions: TermsAndConditions
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>SystemParameters.<b>dParameter</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>DParameter!</b></code>](/midnight-indexer/types/objects/dparameter.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="object"/>

검증자 위원회 구성을 제어하는 D-parameter입니다.

#### [<code style={{ fontWeight: 'normal' }}>SystemParameters.<b>termsAndConditions</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>TermsAndConditions</b></code>](/midnight-indexer/types/objects/terms-and-conditions.mdx) <Badge class="badge badge--secondary " text="object"/>

현재 설정된 이용약관입니다(설정된 경우).

### Member Of

[`Block`](/midnight-indexer/types/objects/block.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
