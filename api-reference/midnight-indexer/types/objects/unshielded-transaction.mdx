---
id: unshielded-transaction
title: UnshieldedTransaction
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)"
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)"
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

UTXO를 생성 및/또는 소비한 트랜잭션으로, 관련 정보를 함께 포함합니다.

```graphql
type UnshieldedTransaction {
  transaction: Transaction!
  createdUtxos: [UnshieldedUtxo!]!
  spentUtxos: [UnshieldedUtxo!]!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>UnshieldedTransaction.<b>transaction</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Transaction!</b></code>](/midnight-indexer/types/interfaces/transaction.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="interface"/>

UTXO를 생성 및/또는 소비한 트랜잭션입니다.

#### [<code style={{ fontWeight: 'normal' }}>UnshieldedTransaction.<b>createdUtxos</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>[UnshieldedUtxo!]!</b></code>](/midnight-indexer/types/objects/unshielded-utxo.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="object"/>

위 트랜잭션에서 생성된 UTXO입니다(없을 수도 있음).

#### [<code style={{ fontWeight: 'normal' }}>UnshieldedTransaction.<b>spentUtxos</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>[UnshieldedUtxo!]!</b></code>](/midnight-indexer/types/objects/unshielded-utxo.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="object"/>

위 트랜잭션에서 소비된 UTXO입니다(없을 수도 있음).

### Implemented By

[`UnshieldedTransactionsEvent`](/midnight-indexer/types/unions/unshielded-transactions-event.mdx) <Badge class="badge badge--secondary badge--relation" text="union"/>
