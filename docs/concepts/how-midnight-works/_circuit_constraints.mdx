```text title="회로 제약 조건"
guess_number:
  inputs:
    public: n1, n2, n3,
      transcript code
    private: a1, b1, a2, b2
  constraints:
    a1 != 1
    b1 != 1
    a1 * b1 = n2
    a2 != 1
    b2 != 1
    n3 = a2 * b2
    // 공개 transcript 형태를
    // 강제하기 위한 추가 제약 조건
```
